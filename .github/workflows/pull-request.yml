name: PR

on:
  pull_request:
    branches: 
      - an-e2e-demo

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
        
    - name: Docker build
      run: |
        docker build -f ${{ github.workspace }}/Dockerfile -t ajinkya599/scanrepo:beta-0.0 .

    - name: Container scan
      uses: Azure/container-scan@uusers/aj/check-run-through-app-test-2
      with:
        image-name: ajinkya599/scanrepo:beta-0.0
